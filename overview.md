# 每日学习打卡工具 - 多项目支持

## 改动内容

### 支持每个时段添加多个打卡项目（2026-07-04更新）

- 每个时段（上午/下午/晚上）可自由添加多个独立项目
- 每个项目包含：时间段（起止时间）、学习计划、涉及科目、完成情况、学习效果
- 涉及科目已改为：**语文、数学、英语、科学、文综**
- 删除按钮：每个项目可独立删除（仅在≥2个项目时显示）
- 时间段自动计算时长

### 双角色模式（前期已实现）

将原来的「家长自己打卡汇总」模式重构为「组长汇总 + 家长填报」双角色模式：

### 角色切换
- 顶部新增「家长填报 / 组长汇总」角色切换按钮
- 不同角色看到不同的界面和功能

### 家长模式
- 填写自己孩子的打卡记录
- 可保存、提交打卡到群共享
- 可查看历史记录

### 组长模式
- 查看群内所有已提交的打卡记录（按日期）
- 显示统计：已提交人数、三段全填人数、总学习时长
- 支持导入家长打卡数据（JSON格式）
- 支持导出汇总数据
- **一键汇总**：生成格式化汇总文本
- 转发给班主任功能

### 数据机制
- 通过 localStorage 模拟群内数据共享
- 家长提交后，数据存储到 `groupCheckin_日期_姓名` 键
- 索引存储在 `groupCheckinIndex`
- 支持导入/导出 JSON 数据实现跨设备汇总

## 使用场景

1. **同一部手机/设备**：直接在钉钉中打开，多人切换角色使用
2. **不同设备**：家长填报后导出 JSON → 传给组长 → 组长导入汇总